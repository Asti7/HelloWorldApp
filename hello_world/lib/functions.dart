import 'dart:async';

import 'package:url_launcher/url_launcher.dart';

class PdfLauncher {
  static launchURLSwift() async {
    const url =
        'https://drive.google.com/file/d/1bysdPg_gCgTuGAfygeg7RuE-mpRDvXJx/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLPython() async {
    const url =
        'https://drive.google.com/file/d/1cXG9-C0NEZFpFpUvK8B1PudtkfgWPZo2/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLRuby() async {
    const url =
        'https://drive.google.com/file/d/10mkPxH1jozQsfxBV1gCJaS4g2kpEaWMa/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLPhp() async {
    const url =
        'https://drive.google.com/file/d/1hO3v5ZEFsEnEKRCrakOE9tGqWhFKJs81/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLJs() async {
    const url =
        'https://drive.google.com/file/d/14YwLPK6UTLORTEWm5v5yQSQpOkjyfkaw/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLJava() async {
    const url =
        'https://drive.google.com/file/d/1a9lTSw24pXEv9JfaAgHw2Hpzzrs9XzlS/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLKotlin() async {
    const url =
        'https://drive.google.com/file/d/17Lr-Du66nV_llg36txg5oeoII_LPww2O/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLHaskell() async {
    const url =
        'https://drive.google.com/file/d/1wv4S4N9NS7ovumxJ_CMVmlE0OUfCT5w9/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLGo() async {
    const url =
        'https://drive.google.com/file/d/11sl37MyZfSS6vmQXF1zx_uzhFuk9MaJ8/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLDart() async {
    const url =
        'https://drive.google.com/file/d/1AmE3Vc1y-VjFFQ45pnkD6hFg0stcWQ-E/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLCsharp() async {
    const url =
        'https://drive.google.com/file/d/1oXw73gUo236TCDIRi48IaKXANs-MOc-3/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLCpp() async {
    const url =
        'https://drive.google.com/file/d/1JMuGjFM-KtEtMTADv00i2r3vwN1iZlce/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  static launchURLC() async {
    const url =
        'https://drive.google.com/file/d/1If07IJGUeIq8ch8dgPwD9nViExYIdPmx/view?usp=sharing';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }
}
